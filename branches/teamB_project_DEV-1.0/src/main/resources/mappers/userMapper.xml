<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.ync.project.mapper.UserMapper">

	<select id="login" resultType="UserVO">
		SELECT	USER_ID,
				USER_PW
		FROM	TB_USER
		WHERE	USER_ID = #{USER_ID}
		AND		USER_PW = #{USER_PW}
	</select>
  
	<update id="keepLogin" >
		UPDATE TB_USER SET sessionKey = #{sessionId}, sessionLimit = #{next} 
		WHERE USER_ID = #{USER_ID}
	</update>

	<select id="checkUserWithSessionKey" resultType="UserVO">
		SELECT 	*
		FROM	TB_USER
		WHERE	sessionKey = #{value}
		AND		sessionlimit > SYSDATE
	</select>	

	<insert id ="insertMember">
    	INSERT into TB_USER (USER_ID, USER_PW, USER_EMAIL) values (#{USER_ID},#{USER_PW},#{USER_EMAIL})
	</insert>
	
</mapper>


